import { Book } from "../shared/book";

const book: Book = {
    title: "test",
    url: "test",
    imageUrl: "test",
    reviews: 500,
    rating: 4.2,
    authors: [],
    year: 2009
};

console.log(book);