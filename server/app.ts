import { Book } from "../shared/book";
import { Constants } from "../shared/constants";


const book: Book = {
    title: "test",
    url: "test",
    imageUrl: "test",
    reviews: 500,
    rating: 4.2,
    authors: [],
    year: 2009
};

console.log(book);
console.log(Constants.APP_NAME);